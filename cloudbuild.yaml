steps:
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/$PROJECT_ID/the-nowl-all-in-one", "."]
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/the-nowl-all-in-one"]
images:
  - "gcr.io/$PROJECT_ID/the-nowl-all-in-one"
